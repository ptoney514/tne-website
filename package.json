{
  "name": "tne_website",
  "version": "1.0.0",
  "type": "module",
  "description": "TNE United Express youth basketball website",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:headed": "npx playwright test --headed",
    "test:unit": "cd react-app && npm run test:unit",
    "lint:react": "cd react-app && npm run lint",
    "build:react": "cd react-app && npm run build",
    "pr-check": "npm run lint:react && npm run build:react && npm run test",
    "import:coaches": "node scripts/import-data.js --coaches",
    "import:teams": "node scripts/import-data.js --teams",
    "import:players": "node scripts/import-data.js --players",
    "import:games": "node scripts/import-data.js --games",
    "import:all": "node scripts/import-data.js --all",
    "seed": "node scripts/seed-from-excel.js",
    "seed:generate": "node scripts/generate-excel-template.js",
    "seed:clear": "node scripts/clear-seed-data.js",
    "seed:reset": "npm run seed:clear -- --confirm && npm run seed"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@supabase/supabase-js": "^2.89.0",
    "csv-parse": "^5.6.0",
    "dotenv": "^16.4.7",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
